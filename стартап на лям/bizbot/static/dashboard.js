(function () {
  if (typeof window === "undefined") return;

  window.BizBotDashboard = {
    version: "0.1.0",
    init: function init() {
      console.log("BizBot dashboard script loaded");
    },
  };
})();
